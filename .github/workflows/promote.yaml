
name: Promote Charm

on:
  workflow_dispatch:
    inputs:
      promotion:
        type: choice
        description: Channel to promote from
        options:
          - edge -> beta
          - beta -> candidate
          - candidate -> stable

jobs:
  promote:
    name: Promote
    uses: canonical/observability/.github/workflows/charm-promote.yaml@b11dfdef528002f1765a87d3c7ab1a771f5bcbe0 # v2
    with:
      promotion: ${{ github.event.inputs.promotion }}
    secrets: inherit