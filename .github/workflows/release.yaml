name: Release to latest/edge

on:
  push:
    branches:
      - main

jobs:
  release:
    uses: canonical/observability/.github/workflows/charm-release.yaml@f09a6a6b1982e7c8d8fb84b1cfd73070b44a1ca2 # v1
    secrets: inherit