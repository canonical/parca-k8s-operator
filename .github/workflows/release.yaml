name: Release to latest/edge

on:
  push:
    branches:
      - main

jobs:
  release:
    uses: canonical/observability/.github/workflows/charm-release.yaml@aa79726b37701487e0808ededd44e9af48651e51 # v1
    secrets: inherit