name: Pull Requests
on:
  pull_request:
    branches:
      - main
      - track/**

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  pull-request:
    name: PR
    uses: canonical/observability/.github/workflows/charm-pull-request.yaml@ad88c000b208a68b2403aaada8f8b01141bdd18b # v1
    secrets: inherit
